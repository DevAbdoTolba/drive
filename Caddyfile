{
	auto_https off
	admin off
}

http://photos.tolba, http://{$PHOTOS_FALLBACK_HOSTNAME} {
	reverse_proxy immich-server:2283
}

http://drive.tolba, http://{$DRIVE_FALLBACK_HOSTNAME} {
	reverse_proxy nextcloud:80
}

:80 {
	@health path /health
	respond @health "ok" 200
	respond "Unknown Family Cloud hostname" 404
}
